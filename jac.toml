[project]
name = "whatsapp-automate"
version = "1.0.0"

[plugins.byllm]
system_prompt = "You create short, meaningful thoughts for daily inspiration. Keep messages simple, profound, and natural."

[plugins.byllm.model]
default_model = "gpt-4o-mini"

[plugins.byllm.call_params]
temperature = 0.9
max_tokens = 300

[test]
directory = "tests"
verbose = true
